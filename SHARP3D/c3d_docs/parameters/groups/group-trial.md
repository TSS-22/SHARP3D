# TRIAL

This group is common in C3D files generated by [Vicon Motion Systems](https://www.vicon.com) and stores the C3D frame range using a method that works around the traditional limit when the [POINT:FRAMES](../required/point/point-frames.md) parameter is written as an unsigned 16-bit integer. While this group is undocumented, the parameters described in this group may be used to support the synchronization of data within the C3D file with [external video files containing odd and even fields of data](https://en.wikipedia.org/wiki/Interlaced_video).

> C3D files store 3D data frames, not [odd and even video fields](https://en.wikipedia.org/wiki/Interlaced_video). As a result the interpretation of the TRIAL group parameters by third-party applications is open to local interpretations.

The total number of 3D frames may be stored in the [POINT:FRAMES](../required/point/point-frames.md) parameter as an unsigned 16-bit integer, which limits the maximum frame count to no more than 65535 frames. When using high-speed video cameras, this translates to a trial of less than five minutes long at 240Hz. To exceed the unsigned 16-bit integer limit, and allow more than 65535 frames to be stored, a new TRIAL group containing two new parameters ([ACTUAL_START_FIELD](../additional/trial/trial-actual_start_field.md) and [ACTUAL_END_FIELD](../additional/trial/trial-actual_end_field.md)) was added to C3D files by [Vicon Motion Systems](https://www.vicon.com) to store the first and last 3D frame numbers of data in the C3D file with a unique 32-bit integer resolution in the TRIAL group.

> All the TRIAL parameters are typically unlocked and have no parameter descriptions documenting their function.

<!--hex dump-->

<!--The structure of typical [ACTUAL_START_FIELD](../additional/trial/trial-actual_start_field.md) and [ACTUAL_END_FIELD](../additional/trial/trial-actual_end_field.md) parameters,
in an Intel formatted C3D file, is shown above, the [ACTUAL_START_FIELD](../additional/trial/trial-actual_start_field.md) (red) is
177 (0x00B1h, 0x0000h) and the [ACTUAL_END_FIELD](../additional/trial/trial-actual_end_field.md) (green) is 1644 (0x066Ch,
0x0000h). -->

This method ignores the possibility of storing the total C3D frame count as a floating-point [POINT:FRAMES](../required/point/point-frames.md) parameter, an option for all software applications that read C3D parameters correctly by determining the parameters type before reading the stored values. This is probably a result of the first C3D user guide describing the way that data was being stored in C3D files at the time that the C3D user guide was written, instead of the way that data could be stored in C3D files.

A potential complication exists because [AMASS](https://wiki.has-motion.com/doku.php?id=other:amass:amass_overview), the first C3D software application, recorded the frame range of the raw data that was used to create each C3D file in the C3D header, resulting in subsequent applications incorrectly assuming that the frame numbers recorded in the C3D header were the frame numbers of the data in the C3D file. This is not the case, while the two sets of numbers are normally related there is no requirement that they match. But as a result of this faulty assumption some software applications may expect that the [TRIAL:ACTUAL_START_FIELD](../additional/trial/trial-actual_start_field.md) and [TRIAL:ACTUAL_END_FIELD](../additional/trial/trial-actual_end_field.md) parameters duplicate the C3D header values.

## Frame Number Calculation From Trial

Thus, when using the TRIAL group parameters to calculate the C3D file size, the
number of frames in the C3D file is calculated using the 32-bit TRIAL group values,
subtracting the results of the [ACTUAL_START_FIELD](../additional/trial/trial-actual_start_field.md) from the [ACTUAL_END_FIELD](../additional/trial/trial-actual_end_field.md)
calculated values **and adding 1**:

$\text{Number of frames} = \text{Last frame number} – \text{First frame number} +1$

The $+ 1$ is because there is no frame 0 in a C3D file, the first frame is always frame 1

A prime example of this, is that if a C3D file contains data from frame $233$ to frame
$79589$ then the C3D frame count is $79357$, as : $79589 – 233 + 1 = 79357$ 

The default [POINT:FRAMES](../required/point/point-frames.md) parameter will normally contain the same frame count unless the count is greater than 65335 in which case [POINT:FRAMES](../required/point/point-frames.md) should be set to 65535 indicating that the integer frame count limit may have been exceeded.